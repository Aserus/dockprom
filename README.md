# dockprom

### Команда для запуска
```
git clone https://github.com/Aserus/dockprom
cd dockprom
docker compose up -d
```

Логи
docker compose logs

порты 8080 и 9100 должны быть свободны

На старых машинах если не работает вместо "docker compose" надо использовать "docker-compose"
